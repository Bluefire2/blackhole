import { defineConfig } from 'vite';

export default defineConfig({
  // This must match the GitHub repository name so that
  // assets are served correctly from GitHub Pages.
  // Your site will be available at:
  // https://<username>.github.io/blackhole/
  base: '/blackhole/',
});


